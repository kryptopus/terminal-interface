import connect from "./connect"
import Header from "../component/Header"

/**
 * Enhance Style component
 * Connect the component to Redux store
 */
export default connect((store, dispatch, ownProps) => {
    return {
    };
})(Header);
